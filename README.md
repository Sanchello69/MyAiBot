# MyAiBot

Android-приложение для общения с AI-ассистентом GigaChat через текстовый чат.

## Описание

MyAiBot — это простое Android-приложение, которое позволяет пользователям общаться с AI-ассистентом GigaChat от Сбера. Приложение реализует полнофункциональный чат-интерфейс с возможностью отправки сообщений и получения ответов от AI.

## Основные возможности

- 💬 Текстовый чат с AI-ассистентом GigaChat
- 🔄 Автоматическое обновление токенов доступа
- 📱 Современный UI на базе Jetpack Compose
- 🔐 Безопасное хранение ключей авторизации
- ⚡ Реактивное управление состоянием с Flow
- 🏗️ Архитектура UDF (Unidirectional Data Flow)

## Стек технологий

### Язык и платформа
- **Kotlin** — основной язык разработки
- **Android** — целевая платформа

### UI и архитектура
- **Jetpack Compose** — современный декларативный UI фреймворк
- **Material Design 3** — дизайн-система для Android
- **ViewModel** — управление UI-связанными данными
- **UDF (Unidirectional Data Flow)** — архитектурный паттерн для управления состоянием

### Асинхронность
- **Kotlin Coroutines** — для асинхронных операций
- **Flow** — реактивные потоки данных
- **StateFlow** — управление состоянием UI

### Сеть и API
- **Retrofit2** — HTTP-клиент для работы с REST API
- **OkHttp** — HTTP-клиент с поддержкой логирования
- **Gson** — сериализация/десериализация JSON

### Dependency Injection
- **Koin** — легковесный DI-фреймворк для Kotlin

### Архитектура
- **Single Activity** — одна активность для всего приложения
- **MVVM** — Model-View-ViewModel архитектура

## Структура проекта

```
app/src/main/java/com/turboguys/myaibot/
├── config/
│   └── AuthConfig.kt              # Конфигурация авторизации (в .gitignore)
├── data/
│   ├── api/
│   │   ├── dto/                    # Data Transfer Objects
│   │   │   ├── GigaChatRequest.kt
│   │   │   ├── GigaChatResponse.kt
│   │   │   ├── OAuthRequest.kt
│   │   │   └── OAuthResponse.kt
│   │   ├── GigaChatApi.kt          # API для чата
│   │   └── GigaChatOAuthApi.kt     # API для OAuth
│   └── repository/
│       └── ChatRepository.kt       # Репозиторий для работы с API
├── di/
│   └── AppModule.kt                # Модуль зависимостей Koin
├── domain/
│   └── model/
│       └── Message.kt              # Доменная модель сообщения
├── presentation/
│   └── chat/
│       ├── ChatAction.kt           # Действия в UDF
│       ├── ChatEvent.kt             # События от UI
│       ├── ChatScreen.kt            # UI экран чата
│       ├── ChatState.kt             # Состояние UI
│       └── ChatViewModel.kt         # ViewModel для чата
├── ui/
│   └── theme/                      # Тема приложения
├── MainActivity.kt                 # Главная активность
└── MyAiBotApplication.kt           # Application класс
```

## Архитектура UDF

Приложение использует паттерн Unidirectional Data Flow (UDF):

1. **State** — состояние UI (`ChatState`)
2. **Event** — события от пользователя (`ChatEvent`)
3. **Action** — действия, которые выполняет ViewModel (`ChatAction`)

Поток данных: `UI → Event → ViewModel → Action → State → UI`

## Настройка проекта

### 1. Получение ключа авторизации

1. Зарегистрируйтесь в [личном кабинете GigaChat](https://developers.sber.ru/)
2. Получите ключ авторизации (Authorization Key)

### 2. Настройка ключа авторизации

1. Откройте файл `app/src/main/java/com/turboguys/myaibot/config/AuthConfig.kt`
2. Замените `YOUR_AUTHORIZATION_KEY_HERE` на ваш реальный ключ авторизации:

```kotlin
object AuthConfig {
    const val AUTHORIZATION_KEY = "ваш_ключ_авторизации_здесь"
}
```

**Важно:** Файл `AuthConfig.kt` добавлен в `.gitignore` и не будет закоммичен в Git.

### 3. Сборка проекта

```bash
./gradlew assembleDebug
```

### 4. Установка на устройство

```bash
./gradlew installDebug
```

## Как работает приложение

### Процесс авторизации

1. При первом запросе приложение получает Access Token через OAuth API
2. Токен кэшируется и используется для последующих запросов
3. Токен действителен 30 минут
4. При получении ошибки 401 токен автоматически обновляется

### Процесс отправки сообщения

1. Пользователь вводит сообщение и нажимает кнопку отправки
2. ViewModel обрабатывает событие `ChatEvent.SendMessage`
3. Repository получает Access Token (если нужно)
4. Отправляется HTTP-запрос к GigaChat API с:
   - Заголовками `X-Request-ID`, `X-Session-ID`, `X-Client-ID`
   - Историей сообщений
5. Ответ от AI отображается в чате

## Особенности реализации

### Безопасность

- Ключ авторизации хранится в отдельном файле, который исключен из Git
- Используется HTTPS для всех сетевых запросов
- Токены доступа кэшируются и автоматически обновляются

### Обработка ошибок

- Автоматическое обновление токена при ошибке 401
- Детальные сообщения об ошибках для пользователя
- Логирование всех HTTP-запросов и ответов (в debug-режиме)

### UI/UX

- Современный Material Design 3 интерфейс
- Адаптивная верстка для разных размеров экранов
- Индикатор загрузки во время обработки запросов
- Автоматическая прокрутка к новым сообщениям
- Поддержка системной навигации (не перекрывается полем ввода)

## Требования

- **Min SDK:** 24 (Android 7.0)
- **Target SDK:** 35 (Android 15)
- **Compile SDK:** 35
- **Kotlin:** 2.0.0
- **Gradle:** 8.8.0

## Зависимости

Основные зависимости проекта:

- `androidx.compose.*` — Jetpack Compose
- `androidx.lifecycle:lifecycle-viewmodel-ktx` — ViewModel
- `org.jetbrains.kotlinx:kotlinx-coroutines-*` — Coroutines и Flow
- `com.squareup.retrofit2:retrofit` — Retrofit2
- `com.squareup.okhttp3:okhttp` — OkHttp
- `io.insert-koin:koin-android` — Koin DI

Полный список зависимостей см. в `gradle/libs.versions.toml`.

## Разработка

### Запуск в режиме отладки

1. Подключите Android-устройство или запустите эмулятор
2. В Android Studio нажмите `Run` или используйте `Shift+F10`
3. Приложение установится и запустится автоматически

### Логирование

Все HTTP-запросы логируются через `HttpLoggingInterceptor`. Для просмотра логов используйте Logcat в Android Studio с фильтром по тегу `OkHttp`.

## Лицензия

Этот проект создан в образовательных целях.

## Контакты

Для вопросов и предложений создайте Issue в репозитории проекта.

---

**Примечание:** Для работы приложения необходим активный ключ авторизации GigaChat API. Получить его можно в [личном кабинете разработчика Сбера](https://developers.sber.ru/).

